version: '3.9'

services:
  chrona-app:
    build:
      context: ./chrona-backend
      dockerfile: Dockerfile
    ports:
      - "8008:8000"
    env_file:
      - ./chrona-backend/.env
  logging-service:
    build: ./logging-service
    ports:
     - "8001:7000"
    env_file:
      - ./logging-service/.env
    ports:
      - 8001:8000